import React from 'react';

const ApiKeySelector: React.FC = () => {
  // As per guidelines, API key is handled via environment variables and no UI should be presented for it.
  // This component is a placeholder.
  return null;
};

export default ApiKeySelector;
